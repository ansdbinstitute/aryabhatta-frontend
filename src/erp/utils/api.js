import client from '../api/client';

/**
 * Global API client for the ERP.
 * This is a proxy to the base axios client in src/erp/api/client.js,
 * provided here for convenience and to maintain consistent import paths
 * across the module-based architecture.
 */
export default client;
